# University RAG Chat - Extension

This extension provides a small popup UI that talks to a local FastAPI server (University RAG Chat). The extension sends requests to the `/ask` endpoint and displays answers.
